<Window
    x:Class="WarfarinManager.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WarfarinManager.UI.ViewModels"
    Title="{Binding Title}"
    Width="1050"
    Height="600"
    MinWidth="900"
    MinHeight="500"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Border
            Grid.Row="0"
            Padding="20,10"
            Background="#2C3E50">
            <TextBlock
                FontSize="18"
                FontWeight="SemiBold"
                Foreground="White"
                Text="WarfarinManager Pro - Gestione Terapia Anticoagulante" />
        </Border>

        <!--  Content Area  -->
        <ContentControl x:Name="ContentArea" Grid.Row="1" />

        <!--  Status Bar  -->
        <Border
            Grid.Row="2"
            Padding="10,5"
            Background="#ECF0F1">
            <TextBlock
                FontSize="11"
                Foreground="#7F8C8D"
                Text="Versione 1.0.0 - Development Build" />
        </Border>
    </Grid>
</Window>
