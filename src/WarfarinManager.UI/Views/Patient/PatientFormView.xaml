<UserControl x:Class="WarfarinManager.UI.Views.Patient.PatientFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WarfarinManager.UI.ViewModels"
             xmlns:local="clr-namespace:WarfarinManager.UI.Converters"
             xmlns:enums="clr-namespace:WarfarinManager.Shared.Enums;assembly=WarfarinManager.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:PatientFormViewModel}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Converters personalizzati -->
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:ErrorToTagConverter x:Key="ErrorToTagConverter"/>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
        
        <!-- Style per TextBox con errore -->
        <Style x:Key="TextBoxWithValidation" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Error">
                    <Setter Property="BorderBrush" Value="#E81123"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#0078D4" 
                Padding="20,15">
            <StackPanel>
                <TextBlock Text="Nuovo Paziente" 
                           FontSize="20" 
                           FontWeight="SemiBold" 
                           Foreground="White"/>
                <TextBlock Text="Inserisci i dati anagrafici del paziente"
                           FontSize="12" 
                           Foreground="White" 
                           Opacity="0.9"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="20">
            <StackPanel MaxWidth="700">
                
                <!-- Sezione Dati Personali -->
                <TextBlock Text="DATI PERSONALI" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Foreground="#0078D4"
                           Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Nome -->
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Nome *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                                 Tag="{Binding FirstNameError, Converter={StaticResource ErrorToTagConverter}}"
                                 Style="{StaticResource TextBoxWithValidation}"/>
                        <TextBlock Text="{Binding FirstNameError}" 
                                   Foreground="#E81123" 
                                   FontSize="11"
                                   Margin="0,3,0,0"
                                   Visibility="{Binding FirstNameError, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Cognome -->
                    <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Cognome *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                                 Tag="{Binding LastNameError, Converter={StaticResource ErrorToTagConverter}}"
                                 Style="{StaticResource TextBoxWithValidation}"/>
                        <TextBlock Text="{Binding LastNameError}" 
                                   Foreground="#E81123" 
                                   FontSize="11"
                                   Margin="0,3,0,0"
                                   Visibility="{Binding LastNameError, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Data di Nascita -->
                    <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,15">
                        <TextBlock Text="Data di Nascita *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding BirthDate}"
                                   Padding="8"
                                   FontSize="13"
                                   BorderBrush="#D0D0D0"
                                   BorderThickness="1"/>
                        <TextBlock Text="{Binding BirthDateError}" 
                                   Foreground="#E81123" 
                                   FontSize="11"
                                   Margin="0,3,0,0"
                                   Visibility="{Binding BirthDateError, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Sesso -->
                    <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,15">
                        <TextBlock Text="Sesso" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding GenderValues}"
                                 SelectedItem="{Binding SelectedGender}"
                                 Padding="8"
                                 FontSize="13"
                                 BorderBrush="#D0D0D0"
                                 BorderThickness="1"/>
                    </StackPanel>

                    <!-- Codice Fiscale -->
                    <StackPanel Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" Margin="0,0,0,15">
                        <TextBlock Text="Codice Fiscale *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding FiscalCode, UpdateSourceTrigger=PropertyChanged}"
                                 MaxLength="16"
                                 CharacterCasing="Upper"
                                 Tag="{Binding FiscalCodeError, Converter={StaticResource ErrorToTagConverter}}"
                                 Style="{StaticResource TextBoxWithValidation}"/>
                        <TextBlock Text="{Binding FiscalCodeError}" 
                                   Foreground="#E81123" 
                                   FontSize="11"
                                   Margin="0,3,0,0"
                                   Visibility="{Binding FiscalCodeError, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </StackPanel>
                </Grid>

                <!-- Separatore -->
                <Border Height="1" Background="#E0E0E0" Margin="0,10,0,20"/>

                <!-- Sezione Contatti -->
                <TextBlock Text="CONTATTI" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Foreground="#0078D4"
                           Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Telefono -->
                    <StackPanel Grid.Column="0" Margin="0,0,0,15">
                        <TextBlock Text="Telefono" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8"
                                 FontSize="13"
                                 BorderBrush="#D0D0D0"
                                 BorderThickness="1"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Grid.Column="2" Margin="0,0,0,15">
                        <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                 Tag="{Binding EmailError, Converter={StaticResource ErrorToTagConverter}}"
                                 Style="{StaticResource TextBoxWithValidation}"/>
                        <TextBlock Text="{Binding EmailError}" 
                                   Foreground="#E81123" 
                                   FontSize="11"
                                   Margin="0,3,0,0"
                                   Visibility="{Binding EmailError, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </StackPanel>
                </Grid>

                <!-- Indirizzo -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Indirizzo" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                             Padding="8"
                             FontSize="13"
                             BorderBrush="#D0D0D0"
                             BorderThickness="1"/>
                </StackPanel>

                <!-- Separatore -->
                <Border Height="1" Background="#E0E0E0" Margin="0,10,0,20"/>

                <!-- Note -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="NOTE ANAMNESTICHE" 
                               FontSize="14" 
                               FontWeight="SemiBold" 
                               Foreground="#0078D4"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                             Height="100"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Padding="8"
                             FontSize="13"
                             BorderBrush="#D0D0D0"
                             BorderThickness="1"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="2" 
                Background="#F5F5F5" 
                Padding="20,15"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right">
                <Button Content="✖ Annulla"
                        Command="{Binding CancelCommand}"
                        Background="#E0E0E0"
                        Foreground="#333333"
                        FontWeight="SemiBold"
                        Padding="20,10"
                        Margin="0,0,10,0"
                        BorderThickness="0"
                        Cursor="Hand"
                        MinWidth="120">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#D0D0D0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="✓ Salva Paziente"
                        Command="{Binding SaveCommand}"
                        Background="#107C10"
                        Foreground="White"
                        FontWeight="SemiBold"
                        Padding="20,10"
                        BorderThickness="0"
                        Cursor="Hand"
                        MinWidth="150"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBoolConverter}}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#0F6B0D"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#A0A0A0"/>
                                    <Setter Property="Cursor" Value="Wait"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" 
              Background="#CC000000"
              Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="💾 Salvataggio in corso..."
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
