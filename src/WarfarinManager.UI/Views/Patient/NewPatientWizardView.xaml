<Window x:Class="WarfarinManager.UI.Views.Patient.NewPatientWizardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:WarfarinManager.UI.ViewModels"
        xmlns:converters="clr-namespace:WarfarinManager.UI.Converters"
        mc:Ignorable="d"
        Title="Configurazione Iniziale Nuovo Paziente"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        d:DataContext="{d:DesignInstance Type=vm:NewPatientWizardViewModel}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="SecondaryButtonStyle"
            BasedOn="{StaticResource PrimaryButtonStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#0078D4" />
            <Setter Property="BorderBrush" Value="#0078D4" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="#0078D4"
                Padding="20,15">
            <StackPanel>
                <TextBlock Text="⚙️ Configurazione Iniziale Paziente"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="White"/>
                <TextBlock FontSize="13"
                           Foreground="White"
                           Opacity="0.9"
                           Margin="0,5,0,0">
                    <Run Text="Paziente:"/>
                    <Run Text="{Binding PatientName, Mode=OneWay}" FontWeight="SemiBold"/>
                </TextBlock>
                <TextBlock Text="Completa i seguenti passaggi obbligatori prima di poter inserire controlli INR"
                           FontSize="12"
                           Foreground="White"
                           Opacity="0.85"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Progress Indicator -->
        <Border Grid.Row="1"
                Background="#F5F5F5"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Progress Bar -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                    <Border Width="35" Height="35"
                            CornerRadius="18"
                            Background="#0078D4"
                            Margin="0,0,10,0">
                        <TextBlock Text="{Binding CurrentStep}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="/ 4"
                               FontSize="14"
                               Foreground="#666666"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Current Step Title -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentStepTitle}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#333333"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Border Grid.Row="2"
                Background="White"
                Padding="20">
            <ContentControl x:Name="StepContentControl"/>
        </Border>

        <!-- Footer with Navigation Buttons -->
        <Border Grid.Row="3"
                Background="#F5F5F5"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Annulla -->
                <Button Grid.Column="0"
                        Content="✖ Annulla"
                        Command="{Binding CancelWizardCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="20,10"
                        FontSize="13"/>

                <!-- Indietro -->
                <Button Grid.Column="2"
                        Content="← Indietro"
                        Command="{Binding PreviousStepCommand}"
                        Visibility="{Binding CanGoPrevious, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="20,10"
                        Margin="0,0,10,0"
                        FontSize="13"/>

                <!-- Avanti -->
                <Button Grid.Column="3"
                        Content="Avanti →"
                        Command="{Binding NextStepCommand}"
                        Visibility="{Binding CanGoNext, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Padding="25,10"
                        FontSize="13"
                        FontWeight="SemiBold"/>

                <!-- Completa -->
                <Button Grid.Column="3"
                        Content="✓ Completa Configurazione"
                        Command="{Binding CompleteWizardCommand}"
                        Visibility="{Binding IsLastStep, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Padding="25,10"
                        FontSize="13"
                        FontWeight="SemiBold"/>
            </Grid>
        </Border>
    </Grid>
</Window>
