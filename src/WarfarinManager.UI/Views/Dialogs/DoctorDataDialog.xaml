<Window
    x:Class="WarfarinManager.UI.Views.Dialogs.DoctorDataDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WarfarinManager.UI.ViewModels"
    Title="Dati del medico"
    Width="550"
    Height="500"
    d:DataContext="{d:DesignInstance Type=vm:DoctorDataViewModel}"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,10,0,5" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock
                FontSize="18"
                FontWeight="Bold"
                Text="Dati del medico" />
            <TextBlock
                Margin="0,5,0,0"
                FontSize="12"
                Foreground="#666"
                Text="Questi dati verranno utilizzati nelle stampe generate dall'applicazione"
                TextWrapping="Wrap" />
        </StackPanel>

        <!--  Form  -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!--  Cognome e nome  -->
                <Label Content="Cognome e nome *" />
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />

                <!--  Indirizzo  -->
                <Label Content="Via e numero civico" Margin="0,20,0,5" />
                <TextBox Text="{Binding Street, UpdateSourceTrigger=PropertyChanged}" />

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <Label Content="CAP" />
                        <TextBox Text="{Binding PostalCode, UpdateSourceTrigger=PropertyChanged}" MaxLength="10" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <Label Content="Città" />
                        <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Grid>

                <!--  Contatti  -->
                <Label Content="Telefono" Margin="0,20,0,5" />
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" MaxLength="20" />

                <Label Content="Email" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="100" />

                <TextBlock
                    Margin="0,20,0,0"
                    FontSize="11"
                    FontStyle="Italic"
                    Foreground="#999"
                    Text="* Campo obbligatorio" />
            </StackPanel>
        </ScrollViewer>

        <!--  Buttons  -->
        <StackPanel
            Grid.Row="2"
            Margin="0,20,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Height="35"
                Margin="0,0,10,0"
                Click="CancelButton_Click"
                Content="Annulla" />
            <Button
                Width="100"
                Height="35"
                Background="#2ECC71"
                Command="{Binding SaveCommand}"
                Content="Salva"
                Foreground="White" />
        </StackPanel>
    </Grid>
</Window>
